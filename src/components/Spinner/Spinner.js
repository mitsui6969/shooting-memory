import React from 'react';
import './Spinner.css'; // 上記のCSSコードを含むファイル

const Spinner = () => {
	return (
    <div className="spinner-container">
      <div className="spinner"></div>
    </div>
	);
};

export default Spinner;
